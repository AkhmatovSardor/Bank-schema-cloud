server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AkhmatovSardor/Bank-schema-config.git
          username: AkhmatovSardor
          password: ${git.password}
        default-label: develop